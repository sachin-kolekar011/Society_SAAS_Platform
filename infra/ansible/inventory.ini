; Replace with the real values from `terraform output ansible_inventory_line`
; after `terraform apply` -- or just run that command and paste its output
; directly here.
[app_servers]
13.205.75.123 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/society-saas-key.pem

[app_servers:vars]
ansible_python_interpreter=/usr/bin/python3
