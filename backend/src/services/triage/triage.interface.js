// suggest(description, availableCategories) -> Promise<{ suggestedCategoryId, suggestedCategoryName, suggestedPriority, confidence }>
// Same Strategy pattern as storage/email (Phase 2) -- callers depend on
// this shape, never on a specific implementation.
module.exports = {};
